type ContactUs
  @model
  @auth(
    rules: [
      { allow: public, operations: [update, delete, create] }
      { allow: owner }
    ]
  ) {
  id: ID!
  email: String!
  firstName: String!
  lastName: String!
  company: String!
  checkBox: [String]
  message: String!
}
